---
phase: 02-onboarding-and-packaging-for-non-technical-users
plan: 03
type: execute
wave: 2
depends_on: [02-02]
files_modified:
  - src/ui/welcome-modal.ts
  - src/main.ts
  - src/settings.ts
  - styles.css
autonomous: true
requirements: []

must_haves:
  truths:
    - "A new file src/ui/welcome-modal.ts exports WelcomeModal extending Modal with a 5-line file-purpose header"
    - "HumaPluginData gains a new field welcomeSeenAt: string | null (ISO timestamp, default null)"
    - "Plugin's onload schedules WelcomeModal to open AFTER startup completes when (data.tokens === null AND data.welcomeSeenAt === null AND startupBlocked === false)"
    - "Welcome modal has 3 steps: Step 1 server URL (read-only with Advanced disclosure), Step 2 Sign in (delegates to SignInModal from plan 02-02), Step 3 first sync result"
    - "On Step 3 completion, welcomeSeenAt is set to new Date().toISOString() and persisted via saveAll"
    - "Sign-out does NOT clear welcomeSeenAt (returning users don't re-onboard)"
    - "resetLocalState clears welcomeSeenAt (full reset means re-onboard)"
    - "Cancel/Esc on Welcome modal does NOT set welcomeSeenAt — user gets the modal again on next enable until they complete or dismiss past sign-in"
    - "Migration: existing data.json without welcomeSeenAt gets null via mergeData; non-null tokens means returning user, modal won't open"
  artifacts:
    - path: "src/ui/welcome-modal.ts"
      provides: "WelcomeModal class with step-based flow"
      contains: "export class WelcomeModal extends Modal"
    - path: "src/settings.ts"
      provides: "welcomeSeenAt field on HumaPluginData"
      contains: "welcomeSeenAt: string | null"
    - path: "src/main.ts"
      provides: "onload trigger that opens WelcomeModal on fresh install"
      contains: "new WelcomeModal"
  key_links:
    - from: "src/ui/welcome-modal.ts WelcomeModal step 2"
      to: "src/ui/sign-in-modal.ts SignInModal (from plan 02-02)"
      via: "Welcome modal closes its own UI, opens SignInModal, awaits onSuccess callback to advance to Step 3"
      pattern: "new SignInModal"
    - from: "src/main.ts onload"
      to: "src/ui/welcome-modal.ts WelcomeModal"
      via: "Conditional new WelcomeModal(...).open() guarded on tokens null + welcomeSeenAt null"
      pattern: "this.data.welcomeSeenAt === null"
---

<objective>
Auto-open a welcome modal on first enable when the user has no tokens stored and has not seen the welcome before. Walks through server URL confirmation → Sign in → first-sync confirmation. Persists a `welcomeSeenAt` flag so it doesn't re-pop on subsequent enables.

Purpose: today the plugin enables silently. A non-technical user sees a faint "Huma" in the status bar and has no idea what to do next. The welcome modal makes the first-run experience self-explanatory.
</objective>

<execution_context>
@/Users/steffen/.claude/get-shit-done/workflows/execute-plan.md
@/Users/steffen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/02-onboarding-and-packaging-for-non-technical-users/BRIEF.md
@src/main.ts
@src/settings.ts
@src/ui/sign-in-modal.ts
@src/ui/stale-resolution-modal.ts
@styles.css

<interfaces>
After plan 02-02, `SignInModal` is available with this dep shape (subject to refinement during 02-02 execution):

```typescript
export interface SignInModalDeps {
    startDeviceFlow: () => Promise<SignInProgress>;
    poll: (sessionId: string) => Promise<DevicePollOutcome>;
    onSuccess: (tokens: StoredTokens) => Promise<void>;
    onCancel: () => void;
    onError: (err: unknown) => void;
}
```

`HumaPluginData` will gain `welcomeSeenAt: string | null` in this plan.

Plugin lifecycle:
- `onload` runs once when the plugin enables. After `loadAll`, `rebuildClients`, status-bar setup, command registration, `onLayoutReady` fires the startup invariant + sync.
- The welcome modal must open AFTER `runStartupInvariant` so blocking conditions (tokens-in-vault, etc.) take precedence and the modal doesn't stack on a startup-blocked Notice.

Existing `mergeData` shape (`src/main.ts:652`):
```typescript
function mergeData(stored): HumaPluginData {
    const base = structuredClone(DEFAULT_PLUGIN_DATA);
    return {
        settings: ...,
        tokens: stored.tokens ?? null,
        manifest: ...,
        // ...
    };
}
```

Project conventions:
- 5-line file-purpose header on welcome-modal.ts (NEW file).
- Tabs for indent.
- Sentence-case UI text.
- Never include "Co-Authored-By: Claude".
- Never run git commands without explicit user permission.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add welcomeSeenAt to plugin data</name>
  <read_first>
    - src/settings.ts (HumaPluginData, DEFAULT_PLUGIN_DATA)
    - src/main.ts § mergeData
  </read_first>
  <behavior>
    Persisted field that records when the user completed the welcome flow. `null` means not yet completed; an ISO timestamp means done.

    Reset paths:
    - Sign-out: do NOT clear welcomeSeenAt (returning user shouldn't re-onboard).
    - resetLocalState: DO clear welcomeSeenAt (full reset = re-onboard).
    - Disable (onunload from plan 02-01): do NOT clear welcomeSeenAt (re-enable shouldn't re-onboard).
  </behavior>
  <action>
    Edit `src/settings.ts`:
    1. Add to `HumaPluginData` interface: `welcomeSeenAt: string | null;` with a comment block explaining its role.
    2. Add to `DEFAULT_PLUGIN_DATA`: `welcomeSeenAt: null,`.

    Edit `src/main.ts` `mergeData`:
    1. Return `welcomeSeenAt: stored.welcomeSeenAt ?? base.welcomeSeenAt,`.

    Edit `src/main.ts` `resetLocalState`:
    1. Add `this.data.welcomeSeenAt = null;` before `await this.saveAll()`.
  </action>
  <verify>
    <automated>cd /Users/steffen/Projects/huma/huma-obsidian-upload && npm run build 2>&1 | tail -5 && npm run lint 2>&1 | tail -3 && npm test 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE 'welcomeSeenAt: string \| null' src/settings.ts` returns one match (interface)
    - `grep -nE 'welcomeSeenAt: null' src/settings.ts` returns one match (default)
    - `grep -nE 'welcomeSeenAt: stored\.welcomeSeenAt' src/main.ts` returns one match (migration)
    - `grep -nE 'this.data.welcomeSeenAt = null' src/main.ts` returns one match (resetLocalState)
    - `npm run build && npm run lint && npm test` all exit 0
  </acceptance_criteria>
  <done>
    `welcomeSeenAt` is part of plugin data, defaults to null, migrates cleanly, and is cleared by resetLocalState only.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create src/ui/welcome-modal.ts</name>
  <read_first>
    - src/ui/sign-in-modal.ts (the modal we delegate to in Step 2)
    - src/ui/stale-resolution-modal.ts (template for layout patterns + busy-state buttons)
    - src/main.ts § signIn, runFullSync (call sites the welcome modal will trigger via deps)
  </read_first>
  <behavior>
    Three-step modal:

    **Step 1 — Server URL.**
    - Header: "Welcome to Huma Vault Sync"
    - Body: "Your notes will sync to the Huma dashboard. Let's get you connected."
    - Server URL display: read-only, large, centered, showing the value of `data.settings.serverBaseUrl`.
    - Disclosure: "Advanced settings ▸". Clicking expands an inline text input pre-filled with the current value. Description: "Most users should leave this as is. Change only if you've been given a custom server."
    - Save behavior: input changes are persisted immediately on each keystroke (via `data.settings.serverBaseUrl = ...; await this.saveAll(); plugin.rebuildHttpClient()`). No save button — same pattern as the existing settings tab.
    - Footer: "Continue" button (Setting `cls: "mod-cta"`). Advances to Step 2.
    - Footer: "Skip for now" button. Closes the modal WITHOUT setting welcomeSeenAt (modal will reappear on next enable).

    **Step 2 — Sign in.**
    - Header: "Sign in"
    - Body: "Click Sign in to authorize the plugin. Your browser will open with a code to enter."
    - Footer: "Sign in" button (mod-cta). Closes welcome modal's UI temporarily (the welcome modal stays in memory but contentEl is empty), opens SignInModal with deps wired so onSuccess/onCancel/onError route back to the welcome modal:
      - `onSuccess`: welcome modal advances to Step 3 (file count from runFullSync result).
      - `onCancel`: welcome modal re-renders Step 2 (user can click Sign in again or Skip).
      - `onError`: welcome modal re-renders Step 2 with an error notice, plus retry copy.
    - Footer: "Skip for now" button.

    **Step 3 — First sync.**
    - Header: "You're connected"
    - Body: result of `runFullSync()`. Three substates:
      - Result has actions: "Synced N files. Status bar at the bottom-right shows current state — click it any time to sync again."
      - Result has zero actions and empty manifest: "No files yet. Open or create a note in this vault and Huma will sync it on the next cycle (every 30 seconds)."
      - Result errored: "Sign-in worked but the first sync failed: {message}. Click Sync now in settings to retry."
    - Footer: "Get started" button (mod-cta). Sets welcomeSeenAt and closes.

    Step transitions are managed via internal `step: 1 | 2 | 3 | "syncing"` state. `render()` rebuilds contentEl based on state.

    Modal cleanup (`onClose`):
    - If welcomeSeenAt was set during the session, do nothing extra.
    - If user dismissed via Esc/X without completing, `welcomeSeenAt` stays null. Modal reappears next enable.

    Public surface:
    ```typescript
    export interface WelcomeModalDeps {
        getServerUrl: () => string;
        setServerUrl: (url: string) => Promise<void>;
        startSignIn: (deps: SignInModalDeps) => void;  // opens SignInModal
        runFirstSync: () => Promise<SyncRunResult | null>;
        markWelcomeSeen: () => Promise<void>;
    }

    export class WelcomeModal extends Modal {
        constructor(app: App, deps: WelcomeModalDeps);
    }
    ```

    The `startSignIn` indirection lets the plugin own the SignInModal construction (with auth client + onSuccess wiring) while the welcome modal just kicks it off.
  </behavior>
  <action>
    Create `/Users/steffen/Projects/huma/huma-obsidian-upload/src/ui/welcome-modal.ts`:

    1. 5-line file-purpose header.
    2. Imports: `App, Modal, Notice, Setting` from obsidian. `type SignInModalDeps` from `./sign-in-modal`. `type SyncRunResult` from `../sync/engine`.
    3. `WelcomeModalDeps` interface as shown above.
    4. `WelcomeModal` class:
       - Constructor takes `app` + `deps`.
       - `state: { step: 1 | 2 | 3 | "syncing"; lastSync: SyncRunResult | null; lastError: string | null } = { step: 1, lastSync: null, lastError: null }`.
       - `onOpen` calls `render()`.
       - `onClose` empties contentEl. Does NOT call markWelcomeSeen (only Step 3's button does that).
       - `private render()` switches on `state.step` and rebuilds contentEl.
       - Step 1 method renders heading, server URL display, disclosure, Continue / Skip buttons.
       - Step 2 method renders heading, body, Sign in / Skip buttons. Sign in closes its own UI (`this.contentEl.empty()`, leave modal open) and calls `deps.startSignIn(signInDeps)` with onSuccess routing back here.
       - Step 3 method renders heading, body (computed from state.lastSync / state.lastError), Get started button which calls `deps.markWelcomeSeen()` then `this.close()`.

    Tabs for indent. Sentence-case UI text. ~180–220 LOC.
  </action>
  <verify>
    <automated>cd /Users/steffen/Projects/huma/huma-obsidian-upload && npm run build 2>&1 | tail -5 && npm run lint 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - File `src/ui/welcome-modal.ts` exists with 5-line file-purpose header
    - `grep -nE 'export class WelcomeModal extends Modal' src/ui/welcome-modal.ts` returns one match
    - `grep -nE 'state\.step === 1' src/ui/welcome-modal.ts` (or equivalent) returns at least one match (step 1 branch)
    - `grep -nE 'state\.step === 2' src/ui/welcome-modal.ts` returns at least one match
    - `grep -nE 'state\.step === 3' src/ui/welcome-modal.ts` returns at least one match
    - `grep -nE 'startSignIn' src/ui/welcome-modal.ts` returns at least one match (delegation to plugin)
    - `grep -nE 'markWelcomeSeen' src/ui/welcome-modal.ts` returns at least one match (Step 3 commit)
    - `npm run build` and `npm run lint` exit 0
  </acceptance_criteria>
  <done>
    `WelcomeModal` is a 3-step welcome flow that delegates the actual sign-in to `SignInModal` from plan 02-02. Step 3 sets welcomeSeenAt; earlier steps do not.
  </done>
</task>

<task type="auto">
  <name>Task 3: Wire WelcomeModal into onload's first-run trigger</name>
  <read_first>
    - src/main.ts § onload, startup, runStartupInvariant
    - src/ui/welcome-modal.ts (Task 2)
  </read_first>
  <behavior>
    The welcome modal opens automatically when ALL of the following are true after startup:
    - `data.tokens === null`
    - `data.welcomeSeenAt === null`
    - `this.startupBlocked === false`

    Trigger location: at the END of `runStartupInvariant`, after the early-return for `startupBlocked` and before any token-based startup decisions. The simpler placement is in `startup()` after `runStartupInvariant` and before the existing `if (this.data.tokens) ... else this.renderStatusBar({ kind: "signed-out" })` block.

    Wire `WelcomeModalDeps`:
    - `getServerUrl`: `() => this.data.settings.serverBaseUrl`
    - `setServerUrl`: `async (url) => { this.data.settings.serverBaseUrl = url.trim().replace(/\/+$/, ""); await this.saveAll(); this.rebuildHttpClient(); }`
    - `startSignIn`: opens `SignInModal` with auth-client deps + an onSuccess that mirrors the existing signIn callback (persist tokens, save, runFullSync, startPolling). Note: this is a duplicate of the logic in `signIn()` — DRY by extracting a `private async finishSignIn(tokens)` helper that both paths call.
    - `runFirstSync`: `async () => { const r = await this.engine?.runSync(); return r ?? null; }`. Caller swallows errors and converts to state.lastError in the welcome modal.
    - `markWelcomeSeen`: `async () => { this.data.welcomeSeenAt = new Date().toISOString(); await this.saveAll(); }`
  </behavior>
  <action>
    Edit `src/main.ts`:

    1. Add import: `import { WelcomeModal } from "./ui/welcome-modal";`
    2. Add a private helper `finishSignIn(tokens: StoredTokens): Promise<void>`:
       ```typescript
       private async finishSignIn(tokens: StoredTokens): Promise<void> {
           this.data.tokens = tokens;
           await this.saveAll();
           new Notice("Huma: signed in.", 3000);
           void this.runFullSync();
           this.startPolling();
       }
       ```
       Refactor `signIn` to call this helper from inside the SignInModal's `onSuccess`.
    3. In `startup()`, after `runStartupInvariant` returns and before the tokens branch, add:
       ```typescript
       if (
           !this.startupBlocked &&
           this.data.tokens === null &&
           this.data.welcomeSeenAt === null
       ) {
           this.openWelcomeModal();
           return;
       }
       ```
    4. Add `private openWelcomeModal()` method that constructs WelcomeModalDeps from the helpers above and `new WelcomeModal(this.app, deps).open()`.
  </action>
  <verify>
    <automated>cd /Users/steffen/Projects/huma/huma-obsidian-upload && npm run build 2>&1 | tail -5 && npm run lint 2>&1 | tail -3 && npm test 2>&1 | tail -3</automated>
    <manual>
      1. Wipe data.json (or set tokens=null and welcomeSeenAt=null manually).
      2. Reload the plugin. Welcome modal opens automatically.
      3. Click Continue → Sign in → complete browser flow → arrive at Step 3.
      4. Click Get started. Modal closes, sync running, status bar idle (or syncing/conflict).
      5. Reload the plugin again. Welcome modal does NOT reopen (welcomeSeenAt is set).
      6. Run "Reset local sync state" command. Reload. Welcome modal opens again (welcomeSeenAt cleared).
      7. Reload (without resetting). Test Skip path: at Step 1, click Skip. Modal closes. Reload. Modal reopens (Skip doesn't set welcomeSeenAt).
    </manual>
  </verify>
  <acceptance_criteria>
    - `grep -nE 'new WelcomeModal' src/main.ts` returns one match
    - `grep -nE 'this.data.welcomeSeenAt === null' src/main.ts` returns at least one match (the gating check)
    - `grep -nE 'private async finishSignIn' src/main.ts` returns one match (DRY helper)
    - `npm run build && npm run lint && npm test` all exit 0
    - Manual verification confirms the full first-run / re-enable / reset cycle behavior
  </acceptance_criteria>
  <done>
    Welcome modal auto-opens on fresh install. Returning users (tokens present OR welcomeSeenAt set) don't see it. Reset clears the flag. Skip preserves the trigger for next enable.
  </done>
</task>

</tasks>

<verification>
- `npm run build && npm run lint && npm test` all exit 0
- Manual first-run cycle: fresh install → welcome modal → sign-in via SignInModal → Step 3 → flag set → re-enable doesn't re-open
- Skip path leaves the modal recurrent until full completion
- Reset re-opens the modal on next enable
</verification>

<success_criteria>
- New users see a guided 3-step flow on first enable, not a blank status bar
- Returning users (signed-in or signed-out post-completion) don't see the welcome modal
- Server URL change in Step 1 persists immediately
- Step 2 reuses SignInModal (zero duplicate UI logic for the device flow)
- Step 3 reports the first-sync result honestly
</success_criteria>

<output>
After completion, create `.planning/phases/02-onboarding-and-packaging-for-non-technical-users/02-03-SUMMARY.md`:

- Lines added across welcome-modal.ts, settings.ts diff, main.ts diff
- Manual verification result for each path: complete, skip, reset, returning user
- Confirmation that finishSignIn is the single source of truth for token-persist + first-sync + start-polling (no duplicated logic)
- Any deviations from the plan
</output>
