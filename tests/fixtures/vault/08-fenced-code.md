# Fenced code

```python
def power_factor(active: float, apparent: float) -> float:
    return active / apparent if apparent else 0.0
```

```ts
export const DEFAULT_INTERVAL_S = 30;
```

Inline `code spans` should round-trip too.
