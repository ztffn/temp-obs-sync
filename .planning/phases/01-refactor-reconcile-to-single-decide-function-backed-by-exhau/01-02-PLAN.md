---
phase: 01-refactor-reconcile-to-single-decide-function-backed-by-exhau
plan: 02
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - docs/CONFLICT-MATRIX.md
autonomous: false

requirements: []

must_haves:
  truths:
    - "docs/CONFLICT-MATRIX.md Verification matrix cites the property-test fixture row name for every master-matrix cell and every sub-matrix #6 cell"
    - "The doc reflects the post-refactor structure: no Pass 1 / Pass 2 / Pass 3 vocabulary; references Step A–E or decide()/dispatch terminology"
    - "All 7 line-number citations of the form reconcile.ts:NNN replaced with symbol-name citations (e.g. `reconcile.ts decide()`, `reconcile.ts dispatch loop`); count drops from 7 to 0"
    - "Build artifacts (main.js + styles.css) copied to /Users/steffen/Projects/huma/Documentation/documentation-library/sources/.obsidian/plugins/huma-vault-sync/"
    - "User has visually verified the dev install loads the refactored plugin without runtime errors and no push_accept entries on unchanged files"
  artifacts:
    - path: "docs/CONFLICT-MATRIX.md"
      provides: "Spec doc whose Verification matrix is derivable from the property-test fixture"
      contains: "decide() property matrix"
    - path: "/Users/steffen/Projects/huma/Documentation/documentation-library/sources/.obsidian/plugins/huma-vault-sync/main.js"
      provides: "Built plugin bundle copied to dev install path"
    - path: "/Users/steffen/Projects/huma/Documentation/documentation-library/sources/.obsidian/plugins/huma-vault-sync/styles.css"
      provides: "Plugin styles copied to dev install path"
  key_links:
    - from: "docs/CONFLICT-MATRIX.md Verification matrix"
      to: "tests/sync/reconcile.test.ts property fixture"
      via: "Each table row names the test it"
      pattern: "decide\\(\\) property matrix"
    - from: "/Users/steffen/Projects/huma/Documentation/documentation-library/sources/.obsidian/plugins/huma-vault-sync/main.js"
      to: "huma-obsidian-upload/main.js (build artifact)"
      via: "cp after npm run build green"
      pattern: ""
---

<objective>
Update `docs/CONFLICT-MATRIX.md` to cite the new property-test fixture for every cell, replace ALL `reconcile.ts:NNN` line-number citations with symbol-name citations (count drops from 7 to 0), refresh section structure to reflect the post-refactor `reconcile.ts` (Step A–E with decide()), then run a final green check (build + lint + test) and copy `main.js` + `styles.css` to the user's dev install path.

Purpose: success criterion #5 in ROADMAP.md requires the Verification matrix in CONFLICT-MATRIX.md to cite the property test for every cell so the doc is derivable from the fixture, not parallel. Success criterion #6 requires the dev install to receive the built artifacts so the user can verify the refactor end-to-end inside Obsidian. Line-number citations rot; the doc itself recommends symbol-name citations in its closing § "Updating this doc" section — this plan enforces that.

Output:
- Updated `docs/CONFLICT-MATRIX.md` with the Verification matrix rewritten to cite the new property-fixture test names; ALL 7 `reconcile.ts:NNN` line-number citations replaced with symbol-name citations.
- `main.js` and `styles.css` copied to `/Users/steffen/Projects/huma/Documentation/documentation-library/sources/.obsidian/plugins/huma-vault-sync/`.
- A human-verify checkpoint where the user opens Obsidian against the dev vault, runs two full sync cycles, and confirms zero `push_accept` entries on files they did not edit.
</objective>

<execution_context>
@/Users/steffen/.claude/get-shit-done/workflows/execute-plan.md
@/Users/steffen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/01-refactor-reconcile-to-single-decide-function-backed-by-exhau/BRIEF.md
@.planning/phases/01-refactor-reconcile-to-single-decide-function-backed-by-exhau/01-01-SUMMARY.md
@docs/CONFLICT-MATRIX.md
@src/sync/reconcile.ts
@tests/sync/reconcile.test.ts

Project conventions to honor:
- Never run git commands without explicit user permission (global CLAUDE.md). The `cp` to the dev install path is a filesystem copy, NOT git, and is explicitly authorized by the brief and ROADMAP success criterion #6.
- Five-line file-purpose header rule applies to NEW source files only. CONFLICT-MATRIX.md is being edited, not created.
- After every code change: `npm run build && npm run lint && npm test` must exit 0 before copying artifacts to the dev install.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update docs/CONFLICT-MATRIX.md to cite the property fixture and reflect Step A–E structure</name>
  <read_first>
    - docs/CONFLICT-MATRIX.md (entire file — every section may need a citation refresh)
    - src/sync/reconcile.ts (post-refactor — get the new symbol names: decide, Step A–E comments, line numbers)
    - tests/sync/reconcile.test.ts (post-refactor — get the exact test names for every property-fixture row)
    - .planning/phases/01-refactor-reconcile-to-single-decide-function-backed-by-exhau/01-01-SUMMARY.md (Plan 01's outcome — fixture row count + any deviations)
  </read_first>
  <action>
    Edit `docs/CONFLICT-MATRIX.md` in place. Three sections need updates; everything else stays.

    1. Section "Master matrix — file existence" (the table after the heading):
       - Replace EVERY `Cite` column entry of the form `reconcile.ts:NNN` or `reconcile.ts:NNN-MMM` with a symbol-name citation. Examples:
           - `reconcile.ts:226` → `reconcile.ts decide() — server-tombstone branch`
           - `reconcile.ts:209-223` → `reconcile.ts decide() — server-live, scan-absent branch`
           - References to dispatch loops → `reconcile.ts dispatch loop — duplicate-uuid guard` / `reconcile.ts dispatch loop — synthetic-entry promotion (Step C)` / `reconcile.ts dispatch loop — server-side rename detection`
       - Use the post-refactor structure: Step A (filter), Step B (duplicate-uuid), Step C (synthetic entry), Step D (dispatch + decide), Step E (no-UUID adds).

    2. Section "Sub-matrix #6 — server live, local present, scan match-by-UUID":
       - Replace the note "rename-local is ordered before pulls and pushes (`reconcile.ts:234-242`)" with `rename-local is ordered before pulls and pushes (reconcile.ts dispatch — server-side rename branch)`.
       - Other in-table citations: same treatment — every `reconcile.ts:NNN` becomes a symbol-name citation.

    3. Section "Verification matrix" (the table at the bottom of the doc):
       - REWRITE this table so every row cites a specific test name from the new `describe("decide() property matrix", ...)` block in `tests/sync/reconcile.test.ts` plus, for back-compat, any pre-existing tests that still cover the row.
       - The new table must have one row per master-matrix cell (#1–#8), one row per sub-matrix #6 boolean combination (8 cells), one row per critical preserved behavior (duplicate-uuid refusal, duplicate-uuid + tombstone, synthetic-entry edit case, synthetic-entry stale-delete case, Pass-2 protection / locally-known-unchanged-no-rep-push), plus the existing rows for excluded-folder, hash-stability, scan filter, manifest fetch policy, etc.
       - Each row's "Test" column must contain the EXACT `describe` + `it` name pair, e.g.: `tests/sync/reconcile.test.ts › decide() property matrix › "master row #3: pulls first-time when server has a row neither local manifest nor scan has"`. Get the names from reading the post-refactor test file.

    4. Section "Updating this doc" — append two sentences:
       > Since the Step A–E refactor (Phase 1), the reconcile decision logic is concentrated in `decide()` and a UUID-union dispatch in `reconcile()`. The Verification matrix below is derivable from the `describe("decide() property matrix", ...)` block in `tests/sync/reconcile.test.ts` — when adding a row to the matrix, add the corresponding fixture row first.

    Run `npm test 2>&1 | grep -E "decide\(\) property matrix" -A 200 | head -300` to capture the live test names from the green run; paste them verbatim into the Verification matrix.

    Pre-refactor citation count is 7 (verified via `grep -cE 'reconcile\.ts:[0-9]+' docs/CONFLICT-MATRIX.md` against the pre-edit file). Post-edit count must be 0 — every line-number citation replaced with a symbol-name citation. Line numbers rot; symbol names don't.

    Do NOT change the master-matrix or sub-matrix structure (column shape, ordering, or wording). Only Cite-column values, the `rename-local is ordered before` line-number note, and the Verification matrix table change. The "Concurrent-divergence semantics" table stays untouched. The "Edge cases and limits" table stays untouched. The "Out of scope / deferred" list stays untouched.
  </action>
  <verify>
    <automated>cd /Users/steffen/Projects/huma/huma-obsidian-upload && grep -c "decide() property matrix" docs/CONFLICT-MATRIX.md && grep -cE 'master row #[1-8]' docs/CONFLICT-MATRIX.md && grep -cE 'sub-matrix #6' docs/CONFLICT-MATRIX.md && grep -cE 'reconcile\.ts:[0-9]+' docs/CONFLICT-MATRIX.md && npm test 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "decide() property matrix" docs/CONFLICT-MATRIX.md` returns at least 1
    - `grep -cE 'master row #[1-8]' docs/CONFLICT-MATRIX.md` returns at least 8 (Verification matrix cites all 8 master-matrix cells with their property-fixture test names)
    - `grep -cE 'sub-matrix #6 [✓✗]' docs/CONFLICT-MATRIX.md` returns at least 8 (one Verification matrix row per boolean combination)
    - `grep -n 'synthetic-server-entry' docs/CONFLICT-MATRIX.md` returns at least 2 lines (push case + stale-local-delete case in Verification matrix)
    - **`grep -cE 'reconcile\.ts:[0-9]+' docs/CONFLICT-MATRIX.md` returns exactly 0** — every reconcile.ts:NNN line-number citation has been replaced with a symbol-name citation (e.g. `reconcile.ts decide()`, `reconcile.ts dispatch loop`). Pre-refactor count was 7; post-refactor count must be 0. Line numbers rot; symbol names don't.
    - `grep -nE '(Pass 1|Pass 2|Pass 3)' docs/CONFLICT-MATRIX.md` returns 0 matches in the master-matrix and sub-matrix sections (the bug-history § "Concurrent-divergence semantics" and similar narrative sections may still mention historical Pass-N if they discuss bug history; verify by hand that any such mention is clearly historical — see Plan 01 SUMMARY)
    - `npm test` still exits 0 (the doc edit cannot break tests, but the green-check confirms nothing accidentally edited a test file)
  </acceptance_criteria>
  <done>
    `docs/CONFLICT-MATRIX.md` is updated: master-matrix and sub-matrix tables cite the post-refactor symbols (decide() / dispatch / Step A–E); ALL 7 pre-refactor `reconcile.ts:NNN` line-number citations replaced with symbol-name citations (post-edit count = 0); the Verification matrix at the bottom rewrites every cell with the exact `describe("decide() property matrix") › it("...")` test-name pair from `tests/sync/reconcile.test.ts`; the closing § "Updating this doc" adds a two-sentence pointer to the property fixture as the source of truth; full test suite still green.
  </done>
</task>

<task type="auto">
  <name>Task 2: Build clean and copy main.js + styles.css to dev install path</name>
  <read_first>
    - .planning/phases/01-refactor-reconcile-to-single-decide-function-backed-by-exhau/BRIEF.md (§ "Acceptance criteria" — the dev install path is named verbatim: `/Users/steffen/Projects/huma/Documentation/documentation-library/sources/.obsidian/plugins/huma-vault-sync/`)
    - .planning/codebase/CONVENTIONS.md § "Code Style" — `npm run build` runs `tsc -noEmit -skipLibCheck && esbuild`
    - package.json (verify the build script and the produced filenames)
  </read_first>
  <action>
    Run a clean build and copy the artifacts. Concrete commands (run from the repo root):

    ```bash
    cd /Users/steffen/Projects/huma/huma-obsidian-upload
    npm run build         # tsc -noEmit + esbuild → produces main.js
    npm run lint          # final lint check
    npm test              # final test green-check
    ```

    Each command must exit 0 before proceeding. If `npm run build` fails, surface the error to the user and STOP — do not copy a broken artifact to the dev install.

    Then verify the destination directory exists and copy:

    ```bash
    DEST="/Users/steffen/Projects/huma/Documentation/documentation-library/sources/.obsidian/plugins/huma-vault-sync"
    test -d "$DEST" || (echo "DEST missing: $DEST" && exit 1)
    cp /Users/steffen/Projects/huma/huma-obsidian-upload/main.js "$DEST/main.js"
    cp /Users/steffen/Projects/huma/huma-obsidian-upload/styles.css "$DEST/styles.css"
    ls -la "$DEST/main.js" "$DEST/styles.css"
    ```

    If `$DEST` does not exist, surface that to the user (the brief's success criterion #6 names the path verbatim — if it's wrong, the user will tell us). Do NOT `mkdir -p $DEST`; the directory is supposed to be a pre-existing Obsidian plugin folder. If absent, the user has restructured something and we need their input.

    Do NOT touch `manifest.json` (no version bump unless the user explicitly asks). Do NOT run `npm version`. Do NOT run any git commands.
  </action>
  <verify>
    <automated>cd /Users/steffen/Projects/huma/huma-obsidian-upload && npm run build 2>&1 | tail -5 && npm run lint 2>&1 | tail -3 && npm test 2>&1 | tail -5 && ls -la "/Users/steffen/Projects/huma/Documentation/documentation-library/sources/.obsidian/plugins/huma-vault-sync/main.js" "/Users/steffen/Projects/huma/Documentation/documentation-library/sources/.obsidian/plugins/huma-vault-sync/styles.css"</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` exits 0
    - `npm run lint` exits 0
    - `npm test` exits 0
    - `test -f /Users/steffen/Projects/huma/Documentation/documentation-library/sources/.obsidian/plugins/huma-vault-sync/main.js` succeeds
    - `test -f /Users/steffen/Projects/huma/Documentation/documentation-library/sources/.obsidian/plugins/huma-vault-sync/styles.css` succeeds
    - The mtime of both copied files is within the last 60 seconds (`find ... -mmin -1` returns both files)
    - `diff /Users/steffen/Projects/huma/huma-obsidian-upload/main.js /Users/steffen/Projects/huma/Documentation/documentation-library/sources/.obsidian/plugins/huma-vault-sync/main.js` shows no diff
    - `diff /Users/steffen/Projects/huma/huma-obsidian-upload/styles.css /Users/steffen/Projects/huma/Documentation/documentation-library/sources/.obsidian/plugins/huma-vault-sync/styles.css` shows no diff
  </acceptance_criteria>
  <done>
    Production build green; freshly built `main.js` and `styles.css` copied to `/Users/steffen/Projects/huma/Documentation/documentation-library/sources/.obsidian/plugins/huma-vault-sync/`; both files match the source tree byte-for-byte.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human-verify dev install loads and syncs without errors (Bug 1 regression gate)</name>
  <what-built>
    - `src/sync/reconcile.ts` refactored to `decide(server, local, scan)` (3-arg) + UUID-union dispatch + Step A–E structure (Plan 01)
    - `tests/sync/reconcile.test.ts` augmented with property-fixture covering every CONFLICT-MATRIX cell (Plan 01)
    - `docs/CONFLICT-MATRIX.md` Verification matrix updated to cite the property fixture; all reconcile.ts:NNN line citations replaced with symbol-name citations (Task 1 above)
    - `main.js` + `styles.css` copied to the dev install path (Task 2 above)
  </what-built>
  <how-to-verify>
    1. Open Obsidian against the documentation-library vault at `/Users/steffen/Projects/huma/Documentation/documentation-library/sources/`. If Obsidian is already open against this vault, fully quit Obsidian (cmd+Q) and reopen — the plugin code is loaded once at vault open and the cached-load path will not pick up the new bundle.
    2. Open Settings → Community plugins → confirm `huma-vault-sync` is enabled. Toggle off and on if it was already enabled, to force-reload the new bundle.
    3. Open the Developer Console (View → Toggle Developer Tools, or cmd+option+I) and clear it.
    4. Run command `Huma Vault Sync: Sync now` (cmd+P) once, then **wait two full sync cycles** (~60s on the default 30s sync interval) without manually editing any files.
    5. Open the audit log modal (command: `Huma Vault Sync: Show sync log`) and inspect the entries from those two cycles.
    6. **Bug 1 regression gate (precise threshold):** Acceptance is **zero `push_accept` audit entries for any file where `scan.hash === local.hash`** (i.e., files the user did not edit between cycles). The previous "handful is fine; dozens is the regression" framing is replaced with this binary check: if any unchanged file produces a `push_accept`, that is a Bug 1 regression and the gate fails. The only legitimate `push_accept` entries are for files the user actually edited (or for first-push of files without prior server entries). For an idle vault across two cycles, the count must be 0.
    7. Also confirm:
       - Status bar transitions through `syncing` → `idle` (or `conflict` if there are pre-existing duplicate-uuid / stale states from prior sessions — these are EXPECTED and not regressions)
       - No red errors in the developer console
       - The audit log shows no `auth_error` or `pull_drop` storm
    8. Compare line-counts: `src/sync/reconcile.ts` should be ≤ 280 LOC post-refactor (vs ~379 LOC pre-refactor). The 01-01-SUMMARY records the actual number.
  </how-to-verify>
  <resume-signal>
    Type `approved` if (a) the plugin loads, (b) two full sync cycles produce zero `push_accept` audit entries for unchanged files, (c) no red console errors. Otherwise, describe the issue (console errors verbatim, audit log signature including the exact `push_accept` entries on unchanged files, suspect file paths) and an explicit decision: `revert` (back out the refactor), `iterate` (open a follow-up task), or `accept-known-issue` (note in 01-02-SUMMARY and ship).
  </resume-signal>
</task>

</tasks>

<verification>
- `docs/CONFLICT-MATRIX.md` Verification matrix cites the property fixture for all 8 master-matrix rows + 8 sub-matrix #6 cells + duplicate-uuid + synthetic-entry rows
- `grep -cE 'reconcile\.ts:[0-9]+' docs/CONFLICT-MATRIX.md` returns 0 (down from 7 pre-refactor)
- `main.js` and `styles.css` exist at the dev install path with mtimes matching the latest build
- User has approved the human-verify checkpoint after running two full sync cycles in the dev vault and observing zero push_accept entries on unchanged files
- All ROADMAP.md Phase 1 success criteria #1–#6 are now demonstrable
</verification>

<success_criteria>
- ROADMAP.md Phase 1 success criterion #5 met: CONFLICT-MATRIX.md "Verification matrix" cites the property test for every cell; doc is derivable from the fixture; all reconcile.ts:NNN citations replaced with symbol-name citations
- ROADMAP.md Phase 1 success criterion #6 met: build artifacts copied to the dev install path
- Human-verify checkpoint passed: plugin loads, two cycles produce zero push_accept on unchanged files, no console errors
- Phase 1 complete; ROADMAP.md Progress row can be updated to reflect 2/2 plans done
</success_criteria>

<output>
After completion, create `.planning/phases/01-refactor-reconcile-to-single-decide-function-backed-by-exhau/01-02-SUMMARY.md` recording:
- Final test count (`npm test` output line)
- Diff size of `docs/CONFLICT-MATRIX.md` (lines added / removed)
- Confirmation that `grep -cE 'reconcile\.ts:[0-9]+' docs/CONFLICT-MATRIX.md` returns 0 (citation rot eliminated)
- Confirmation that `main.js` and `styles.css` are byte-identical between source tree and dev install
- Human-verify outcome (`approved` / issue + resolution), including the count of `push_accept` audit entries observed across two sync cycles
- Any remaining concerns to surface to the next phase (e.g. orphan-recovery command from CONCERNS.md remains deferred)
</output>
